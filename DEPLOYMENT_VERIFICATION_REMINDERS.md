# تعليمات النشر - نظام تذكير التحقق من البريد الإلكتروني

## 📋 الخطوات المطلوبة على الاستضافة

### 1️⃣ رفع الملفات
قم برفع جميع الملفات المعدلة إلى الاستضافة عبر FTP أو Git.

### 2️⃣ تحديث Dependencies (إن لزم الأمر)
```bash
composer install --no-dev --optimize-autoloader
```

### 3️⃣ تنظيف الكاش
```bash
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
php artisan optimize:clear
```

### 4️⃣ إعادة تحميل الكاش (اختياري - للأداء)
```bash
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan optimize
```

### 5️⃣ إعداد Cron Job (مهم جداً ⚠️)

لتفعيل الإرسال التلقائي كل 3 أيام، يجب إضافة Cron Job في cPanel:

#### الطريقة الأولى: عبر cPanel Cron Jobs
1. اذهب إلى **cPanel → Cron Jobs**
2. اختر **Standard (cPanel v54)**
3. أضف السطر التالي:

```bash
* * * * * cd /home/username/public_html && php artisan schedule:run >> /dev/null 2>&1
```

**ملاحظة:** استبدل `/home/username/public_html` بمسار المشروع الفعلي على الاستضافة.

#### الطريقة الثانية: عبر SSH
```bash
crontab -e
```

ثم أضف:
```bash
* * * * * cd /path/to/your/project && php artisan schedule:run >> /dev/null 2>&1
```

#### الطريقة الثالثة: Cron Job مخصص (بدون schedule:run)
إذا لم تستطع استخدام `schedule:run`، يمكنك إضافة Cron Job مباشرة:

```bash
0 9 */3 * * cd /path/to/your/project && php artisan students:send-verification-reminders >> /dev/null 2>&1
```

هذا سيعمل كل 3 أيام في الساعة 9:00 صباحاً (UTC).

### 6️⃣ اختبار النظام

#### اختبار Command يدوياً:
```bash
php artisan students:send-verification-reminders
```

يجب أن ترى رسالة مثل:
```
Starting verification reminder process...
Found X unverified student(s) to send reminders to.
✓ Reminder sent to: student@example.com (Registered 3 days ago)
...
=== Summary ===
Total students checked: X
Reminders sent: X
Failed: 0
```

#### اختبار من لوحة الإدارة:
1. سجل دخول إلى لوحة الإدارة
2. اذهب إلى: **Students → Verification Reminders**
3. جرب إرسال تذكير لطالب واحد
4. تحقق من وصول الرسالة

### 7️⃣ التحقق من Queue (إن كان مفعلاً)

إذا كنت تستخدم Queue، تأكد من تشغيل Worker:

```bash
php artisan queue:work --tries=3 --timeout=90
```

أو إذا كنت تستخدم Supervisor:
```bash
sudo supervisorctl restart laravel-worker:*
```

### 8️⃣ التحقق من إعدادات البريد الإلكتروني

تأكد من أن إعدادات البريد الإلكتروني صحيحة:
1. اذهب إلى: **Admin → Email Configuration**
2. اختبر إرسال رسالة تجريبية
3. تأكد من أن SMTP أو SendGrid يعمل بشكل صحيح

---

## 🔍 استكشاف الأخطاء

### المشكلة: لا يتم إرسال التذكيرات تلقائياً

**الحل:**
1. تحقق من أن Cron Job يعمل:
   ```bash
   php artisan schedule:list
   ```
2. تحقق من Logs:
   ```bash
   tail -f storage/logs/laravel.log
   ```
3. اختبر Command يدوياً:
   ```bash
   php artisan students:send-verification-reminders
   ```

### المشكلة: رسالة خطأ في Cron Job

**الحل:**
- تأكد من المسار الصحيح للمشروع
- تأكد من صلاحيات الملفات (755 للمجلدات، 644 للملفات)
- تأكد من أن PHP CLI متوفر في المسار

### المشكلة: الرسائل لا تصل

**الحل:**
1. تحقق من إعدادات البريد في `.env`
2. تحقق من Spam/Junk folder
3. راجع Logs:
   ```bash
   tail -f storage/logs/laravel.log | grep "verification reminder"
   ```

---

## 📝 ملاحظات مهمة

1. **الوقت:** التذكيرات تُرسل كل 3 أيام في الساعة 9:00 صباحاً (UTC)
2. **الطلاب المستهدفون:** فقط الطلاب غير المتحققين المسجلين قبل 3 أيام أو أكثر
3. **التكرار:** يتم إرسال التذكير كل 3، 6، 9، 12... يوم
4. **Queue:** الرسائل تُرسل عبر Queue لتحسين الأداء
5. **Cache:** يتم استخدام Cache لتحسين الأداء (60 ثانية)

---

## ✅ قائمة التحقق النهائية

- [ ] تم رفع جميع الملفات
- [ ] تم تشغيل `composer install`
- [ ] تم تنظيف الكاش
- [ ] تم إضافة Cron Job
- [ ] تم اختبار Command يدوياً
- [ ] تم اختبار الإرسال من لوحة الإدارة
- [ ] تم التحقق من وصول الرسائل
- [ ] تم التحقق من Logs

---

## 🆘 الدعم

إذا واجهت أي مشاكل:
1. راجع Logs في `storage/logs/laravel.log`
2. تحقق من إعدادات البريد الإلكتروني
3. تأكد من أن Cron Job يعمل بشكل صحيح
4. اختبر Command يدوياً للتأكد من عمله

---

**تاريخ الإنشاء:** {{ date('Y-m-d') }}
**الإصدار:** 1.0

