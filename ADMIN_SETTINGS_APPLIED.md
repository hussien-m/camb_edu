# تطبيق الإعدادات على لوحة التحكم

## ✅ ما تم تطبيقه:

### 1. **اسم الموقع (Site Name)**
- ✅ في عنوان المتصفح (Browser Title)
- ✅ في شعار الـ Sidebar
- ✅ في صفحة تسجيل الدخول

**الاستخدام:**
```blade
{{ setting('site_name', 'Cambridge College') }}
```

### 2. **شعار الموقع (Logo)**
- ✅ في الـ Sidebar (بجانب اسم الموقع)
- ✅ في صفحة تسجيل الدخول (فوق النموذج)

**الاستخدام:**
```blade
@if(setting('site_logo'))
    <img src="{{ asset('storage/' . setting('site_logo')) }}" alt="Logo">
@endif
```

### 3. **أيقونة الموقع (Favicon)**
- ✅ في جميع صفحات لوحة التحكم
- ✅ في صفحة تسجيل الدخول

**الاستخدام:**
```blade
@if(setting('site_favicon'))
    <link rel="icon" type="image/x-icon" href="{{ asset('storage/' . setting('site_favicon')) }}">
@endif
```

### 4. **نص الفوتر (Footer Text)**
- ✅ في أسفل جميع صفحات لوحة التحكم

**الاستخدام:**
```blade
{{ setting('footer_text', 'Default Footer Text') }}
```

## 📍 الملفات المعدلة:

1. ✅ `resources/views/admin/layouts/app.blade.php`
   - إضافة favicon
   - تحديث عنوان الصفحة
   - تحديث نص الفوتر
   - إضافة CSS للشعار

2. ✅ `resources/views/admin/layouts/sidebar.blade.php`
   - إضافة الشعار
   - تحديث اسم الموقع

3. ✅ `resources/views/admin/auth/login.blade.php`
   - إضافة الشعار
   - إضافة favicon
   - تحديث اسم الموقع
   - إضافة CSS للشعار

## 🎯 كيفية تغيير الإعدادات:

### من لوحة التحكم:
1. اذهب إلى: `http://camp.site/admin/settings`
2. قم برفع الشعار في قسم "General Settings"
3. قم برفع الـ Favicon
4. قم بتحديث اسم الموقع
5. قم بتحديث نص الفوتر
6. اضغط "Save Settings"

### من الكود مباشرة:
```php
use App\Models\Setting;

// تحديث اسم الموقع
Setting::set('site_name', 'اسم موقعك الجديد');

// تحديث نص الفوتر
Setting::set('footer_text', '© 2025 موقعك. جميع الحقوق محفوظة.');
```

## 📝 ملاحظات:
- جميع الإعدادات محفوظة في جدول `settings`
- يتم استخدام Cache للإعدادات (مدة 1 ساعة)
- عند التحديث من لوحة التحكم، يتم مسح الـ Cache تلقائياً
- الشعار والـ Favicon يُحفظان في `storage/app/public/settings`
